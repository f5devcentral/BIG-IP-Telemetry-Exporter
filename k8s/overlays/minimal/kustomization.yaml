apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: delete-ingress.yaml

images:
  - name: bigip-telemetry-exporter
    newName: bigip-telemetry-exporter
    newTag: latest
